const fullText = "JavaScript is fun";
const keyword = "fun";
const contains = fullText.includes(keyword);
console.log(contains); // true
// Note: includes returns true if keyword exists in the string[web:1]
